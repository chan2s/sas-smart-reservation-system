# SAS RESERVE — backend environment configuration
# Copy this file to .env and fill in the real values.
# NEVER commit the real .env file.

# ---------------------------------------------------------------------------
# Django
# ---------------------------------------------------------------------------

DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1


# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# Optional — falls back to SQLite when unset.
#
# DATABASE_URL=postgres://user:password@localhost:5432/sas_reserve


# ---------------------------------------------------------------------------
# CORS
# ---------------------------------------------------------------------------

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173


# ---------------------------------------------------------------------------
# Google OAuth
# ---------------------------------------------------------------------------
# Create these credentials in:
# Google Cloud Console → APIs & Services → Credentials
#
# OAuth client type:
# Web application
#
# IMPORTANT:
# Never put the real client secret in this example file.
# Never commit the real .env file.

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret


# ---------------------------------------------------------------------------
# Email (SMTP) — used for first-time Google sign-in verification codes
# and reservation status notifications.
# ---------------------------------------------------------------------------
# For Gmail: create an App Password (Google Account → Security →
# 2-Step Verification → App passwords) and use it as EMAIL_HOST_PASSWORD.
#
# IMPORTANT:
# Real credentials live ONLY in backend/.env (never committed).
# This file must contain placeholders only.

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=SAS RESERVE <your-email@gmail.com>

# Email notifications for reservations are sent to the authenticated user's
# registered email address. Phone numbers are no longer collected.


# ---------------------------------------------------------------------------
# Frontend
# ---------------------------------------------------------------------------
# Public origin of the React application.
# Used by Django when redirecting back to the frontend after OAuth.

PUBLIC_FRONTEND_URL=http://localhost:5173